import React from 'react';

import './style.scss';

const CommentList = props => {
    const data = props.data;
    console.log(data)
    return (
        <div>
            CommentList
        </div>
    )
};

export default CommentList;
