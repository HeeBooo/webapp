import React, { PureComponent  } from 'react';

import './style.scss';

class Comment extends PureComponent {
    render() {
        return (
            <div>
                Comment
            </div>
        )
    };
};

export default Comment;
